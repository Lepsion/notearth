---
{"dg-publish":true,"permalink":"/НеКартонка/Герои/Персонажи/","noteIcon":"","created":"2026-02-05T16:59:32.499+03:00","updated":"2026-02-05T20:02:27.738+03:00"}
---

``` DataviewJS
const pages = dv.pages('#Персонаж');

dv.el('div', `<div style="display:flex; flex-wrap:wrap; gap:10px;">`, false);

pages.forEach(page => {
    dv.el('div', `
        <div style="display:inline-block; border:1px solid #ccc; border-radius:8px; padding:10px; width:300px; vertical-align:top;">
            ${page.Аватарка ? `<div style="text-align:center; margin-bottom:10px;">
                <img src="${page.Аватарка}" style="width:100%; max-height:400px; object-fit:contain; border-radius:5px;">
            </div>` : ''}
            <div style="display:flex; flex-direction:column; gap:4px;">
                ${page.Стойкость ? `<div style="display:flex; justify-content:space-between;"><strong>Стойкость:</strong> <span>${page.Стойкость}</span></div>` : ''}
                ${page.Сила ? `<div style="display:flex; justify-content:space-between;"><strong>Сила:</strong> <span>${page.Сила}</span></div>` : ''}
                ${page.Актив ? `<div><strong>Актив:</strong><br>${page.Актив.map(a => `- ${a}`).join('<br>')}</div>` : ''}
            </div>
        </div>
    `, false);
});

dv.el('div', `</div>`, false);

```